# PrimitiveTypeUtils

> Relevant classes:
> <br>· `com.cleanroommc.kirino.utils.PrimitiveTypeUtils`

It provides a set of utility functions for primitive type and wrapped primitive type related operations.

Common usage pattern:
- `PrimitiveTypeUtils.looseTypeCheck(...) // ignores the difference between primitives and wrapped primitives`
- `PrimitiveTypeUtils.toWrappedPrimitive(...)`
- `PrimitiveTypeUtils.toPrimitive(...)`
